{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard{% endblock %}

{% block body %}
    <section class="panel panel-narrow">
        <p class="eyebrow">Admin</p>
        <h1>Admin Dashboard</h1>

        <div class="stack">
            <a class="button" href="{{ path('app_admin_user_index') }}">Manage Users</a>
            <a class="button button-secondary" href="{{ path('app_admin_firmware_index') }}">Manage Firmware</a>
            <a class="button button-secondary" href="{{ path('app_logout') }}">Logout</a>
        </div>
    </section>
{% endblock %}
